services:
  redis:
    image: redis:latest
    container_name: redis_container
    ports:
      - "6379:6379"
    environment:
      - REDIS_PASSWORD=your_redis_password
    volumes:
      - redis_data:/data

networks:
  docker-network:
    driver: bridge

volumes:
  redis_data:
